import pandas as pd

def size_positions_and_apply_costs(signals_df: pd.DataFrame, crypto_cap: float = 0.05) -> pd.DataFrame:
    # Put your vol-targeting + costs here; stub passes through for now
    return signals_df.copy()
